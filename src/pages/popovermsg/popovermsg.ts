import { Component } from '@angular/core';
import { PopoverController, NavParams } from 'ionic-angular';
@Component({
    selector: 'pop-over-msg',
    templateUrl: 'popovermsg.html',
})

export class PopOverMsg {

  constructor(private navParams: NavParams) {

  }
}
